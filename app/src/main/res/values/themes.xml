<resources>
    <!-- Base application theme. -->
    <style name="Theme.Palabros" parent="@android:style/Theme.Material.Light.NoActionBar">
        <item name="android:statusBarColor">@color/status_bar</item>
    </style>
</resources>
